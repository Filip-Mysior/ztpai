<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Fiszlet{% endblock %}</title>
        <link rel="icon" href="{{ asset('assets/logo2.png') }}">
        
        {% block stylesheets %}
            <link href="{{ asset('css/main.css') }}" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <header>
            <a href="{{ path('app_index') }}">Home</a>
            <a href="{{ path('app_login') }}">Login</a>
            <a href="{{ path('app_register') }}">Register</a>
            <a href="{{ path('app_set') }}">Set Page</a>
            <a href="{{ path('app_set_learn') }}">Learn Page</a>
            <a href="{{ path('app_my_account') }}">My Account</a>
        </header>
        <div class="mainContent">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
